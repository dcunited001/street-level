-# this might be a case of too much refactoring
= render :partial => 'pages/article', :locals => { :article => { :name => 'Sponsors',
  :desc => render(:partial => 'sponsor_index', :collection => @sponsors, :as => :sponsor) } }
